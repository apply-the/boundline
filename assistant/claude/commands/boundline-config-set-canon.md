# Command: /boundline-config-set-canon

Shared guidance: `assistant/README.md`

Map to `boundline config set-canon --mode-selection <manual|auto-confirm|auto>`. Ask for the preference only when missing, then run the command and verify with `/boundline-config-show`.
