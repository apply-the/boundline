# Command: /boundline-config-show

Shared guidance: `assistant/README.md`

Run `boundline config show --scope workspace`. Preserve `[canon]` preferences, `mode_selection`, governance defaults, assistant runtimes, model routes, and effective routing fields exactly.
