[package]
name = "boundline-protocol-standalone-consumer"
version = "0.0.0"
edition = "2024"
publish = false
rust-version = "1.96.0"

[dependencies]
boundline-protocol = { path = "../../.." }

[workspace]
